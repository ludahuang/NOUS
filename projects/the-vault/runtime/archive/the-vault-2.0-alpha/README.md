# The Vault 2.0-alpha (Archived)

This folder preserves the stopped `2.0-alpha` cloud shell.

Status:

- archived
- not the active product surface
- kept only for salvageable engineering work

Contents:

```text
apps/web/
docs/
```

The active product is still the static `1.1-dev` app at the repo root.

If this archive is revisited later, use the root-level archive scripts:

```bash
npm run dev:alpha
npm run build:alpha
npm run db:init:alpha
npm run worker:alpha
npm run smoke:alpha
```
