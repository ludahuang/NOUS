# Agents

Agents are active participants with identity, authority, resources, tools,
memory, and evaluation records.

An agent implementation must not be reduced to a UI widget or prompt file.
It should remain inspectable as a cultural and technical actor.

The first active definition is
[`weme-creative-agent/`](weme-creative-agent/).
